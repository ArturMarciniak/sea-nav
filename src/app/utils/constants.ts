export const sourceFilePath = 'assets/data/web_challenge.csv';

export const tileLayerUrl: string =
  'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png';

export const tileLayerOptions: L.TileLayerOptions = {
  maxZoom: 18,
  minZoom: 3,
};
